---
name: dades
description: Deprecated alias. The company-disk accident-prevention layer is now the skill `company-hd`. Use that.
---

# dades → company-hd

This name was Catalan for "data". The skill is now **`company-hd`**: a code-level
read-only gateway that prevents accidents on a company hard drive / NAS.

Use `company-hd`. This stub exists so old prompts still find a pointer.
