# Floors are set to the first release that carries the relevant security fix, and each
# dependency is bounded below the next major so an install cannot silently resolve across
# a breaking boundary. Regenerate with a lockfile (uv/pip-tools) for reproducible installs.

# >=0.109.1 — earlier releases pull a python-multipart vulnerable to CVE-2024-24762
# (ReDoS on multipart form parsing). The previous floor of 0.100.0 permitted it.
fastapi>=0.109.1,<1.0
uvicorn>=0.30.0,<1.0
httpx>=0.27.0,<1.0
pydantic>=2.7.0,<3.0
