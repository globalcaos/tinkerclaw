# SESSION.md — thin dispatcher on `/new` (day-0)

Copied into `~/.openclaw/workspace/SESSION.md` on first setup if missing.

On `/new` or `/reset`:

1. Read `BRIEFING.md` if it exists (workspace copy, else the bundled default).
2. Read `HEARTBEAT.md` if it exists.
3. Produce the briefing. Then stop. Do not start a side quest.
