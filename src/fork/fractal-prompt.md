# FRACTAL REFLECTION PROMPT — MOVED (do not edit this file)

The LIVE fractal-reflection prompt is:

    extensions/tinkerclaw-fractal-reflection/fractal-prompt.md

It is loaded by the **tinkerclaw-fractal-reflection** plugin (v4, gated by
`fork.cognitive.fractal="extension"`) — the active fractal injector that produces the
🌿 FRACTAL section.

This file (`src/fork/fractal-prompt.md`) is read ONLY by the SUPERSEDED v3 path in
`src/fork/attempt-hooks.ts` (`loadFractalPrompt`), which no longer fires: the inline
injection was deleted as dead code (see the "FRACTAL REFLECTION v4 — moved to the plugin"
note in attempt-hooks.ts, ~line 879). The standalone duplicate `src/fork/fractal-inject.ts`
was removed in the same cleanup.

➡️ Edit the extension's file above. Do not add prompt content here.
