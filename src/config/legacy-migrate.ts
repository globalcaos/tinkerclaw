/**
 * STUB: Legacy config migration — upstream file not yet in this merge.
 * TODO: Remove when upstream ships the real implementation.
 */
export function migrateLegacyConfig(_config: Record<string, unknown>): {
  config: Record<string, unknown> | null;
} {
  return { config: null };
}
