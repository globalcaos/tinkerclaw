/**
 * STUB: Command obfuscation detection — upstream file not yet in this merge.
 * TODO: Remove when upstream ships the real implementation.
 */
export function detectCommandObfuscation(command: string): {
  detected: boolean;
  reasons: string[];
} {
  void command;
  return { detected: false, reasons: [] };
}
